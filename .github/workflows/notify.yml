name: OS-Level Push Engine V11

on:
  push:
    paths:
      - './config.json' # يعمل فقط عند تعديل هذا الملف

jobs:
  send_fcm_notification:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install google-auth-library

      - name: Trigger FCM HTTP v1 API
        env:
          SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          node -e "
          const fs = require('fs');
          const https = require('https');
          const { GoogleAuth } = require('google-auth-library');

          async function sendPush() {
            try {
              // 1. قراءة قاعدة البيانات السحابية
              const config = JSON.parse(fs.readFileSync('config.json', 'utf8'));
              if (!config.notifications || config.notifications.length === 0) return;
              
              // سحب أحدث إشعار
              const latestNotif = config.notifications[0];
              console.log('Targeting Notification:', latestNotif.title);

              // 2. تهيئة المصادقة مع جوجل
              const credentials = JSON.parse(process.env.SERVICE_ACCOUNT_JSON);
              const auth = new GoogleAuth({
                credentials,
                scopes: ['https://www.googleapis.com/auth/firebase.messaging']
              });
              
              const client = await auth.getClient();
              const tokenResponse = await client.getAccessToken();
              const accessToken = tokenResponse.token;
              const projectId = credentials.project_id;

              // 3. بناء حمولة الإشعار لنظام التشغيل (OS Payload)
              const payload = JSON.stringify({
                message: {
                  topic: 'all_users',
                  notification: {
                    title: latestNotif.title,
                    body: latestNotif.body,
                    image: latestNotif.imageUrl
                  },
                  data: {
                    targetId: latestNotif.targetId || '',
                    payload: latestNotif.payload || '',
                    click_action: 'FLUTTER_NOTIFICATION_CLICK'
                  },
                  webpush: {
                    fcm_options: { link: 'https://myapp.com' }
                  }
                }
              });

              // 4. إطلاق النبضة
              const options = {
                hostname: 'fcm.googleapis.com',
                path: '/v1/projects/' + projectId + '/messages:send',
                method: 'POST',
                headers: {
                  'Authorization': 'Bearer ' + accessToken,
                  'Content-Type': 'application/json',
                  'Content-Length': Buffer.byteLength(payload)
                }
              };

              const req = https.request(options, (res) => {
                let data = '';
                res.on('data', (chunk) => data += chunk);
                res.on('end', () => console.log('FCM Response:', data));
              });

              req.on('error', (e) => console.error('FCM Error:', e));
              req.write(payload);
              req.end();

            } catch (error) {
              console.error('Fatal Error:', error);
              process.exit(1);
            }
          }
          sendPush();
          "
